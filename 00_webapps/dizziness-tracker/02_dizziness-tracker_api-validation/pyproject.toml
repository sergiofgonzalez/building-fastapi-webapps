[project]
name = "01-dizziness-tracker-hello-fastapi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["fastapi[standard]>=0.112.2"]

[tool.ruff]
# Set the maximum line length to 80
line-length = 80

[tool.ruff.lint]
# # Add the `line-too-long` rule to the enforced rule set.
# extend-select = ["E501"]

select = ["ALL"]

# ignore = [
#   "ANN201"
# ]

# select = [
#   "E",   # pycodestyle
#   "W",   # pycodestyle warning
#   "F",   # Pyflakes
#   "UP",  # pyupgrade
#   "B",   # flake8-bugbear
#   "SIM", # flake8-simplify
#   "I",   # isort
#   "C4",  # pyupgrade
#   "N",   # pep8-naming
#   "P",   # pydocstyle
# ]
